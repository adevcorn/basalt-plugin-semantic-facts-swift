[package]
name = "basalt-plugin-semantic-facts-swift"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
basalt-plugin-sdk = { path = "../basalt-plugin-sdk" }

[profile.release]
opt-level = "s"
lto = true
strip = true
codegen-units = 1
